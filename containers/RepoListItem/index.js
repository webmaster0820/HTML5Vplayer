import { connect } from 'react-redux';
import { createStructuredSelector } from 'reselect';
import SegsListItem from './SegsListItem';

export default connect(
  createStructuredSelector({
  })
)(SegListItem);
