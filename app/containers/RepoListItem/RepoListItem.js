

import React from 'react';
import PropTypes from 'prop-types';
import ListItem from 'components/ListItem';
import { IssueIcon } from 'components/Icons';
import './style.scss';

export default class SegsListItem extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  render() {
    let nameprefix = '';

    const content = (
      <div className="segs-list-item">
      </div>
    );

    // Render the content into a list item
    return (
      <div/>
    );
  }
}

